{
  "name": "bryntum-gantt-experiment",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "(cd backend && npm run start) & (cd frontend && npm run start)",
    "build": "(cd backend && npm run build) & (cd frontend && npm run build)"
  }
}
